# Expected conversion behavior

- Preserve YAML front matter as metadata.
- Keep the body heading and paragraph separate from metadata.
- Do not render the front matter as ordinary body text unless metadata rendering is explicitly requested.
